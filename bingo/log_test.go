package bingo

import "testing"

// 日志单元测试

func Test_Log_set(t *testing.T) {
	//l := new(Log)
	// 设置时区
	// 设置书写模式
	// 设置日志路径
	// 设置文件分割模式
	// 构建协程池等

	// 创建一个日志连接器，只要实现了该接口的结构体都可以传入，里面定义了如何分割文件，如何设置协程等
}
